<view class="container">
  <image bind:tap="onLeft" src="{{latest ? leftDis : left}}" class="icon" />
  <text class="title">{{title}}</text>
  <image bind:tap="onRight" src="{{first ? rightDis : right}}" class="icon" />
</view>